/* global describe, it */

describe('Filter Properties Plugin', function () {
  var fixture = document.getElementById('fixture');

  it('should exist', function () {
    expect(filterPropertiesPlugin).to.exist;
  });
});
